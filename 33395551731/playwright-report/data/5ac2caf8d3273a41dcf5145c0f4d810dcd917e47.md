# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: future-booking-only/future-confirmation.spec.ts >> Future Booking — Confirmation Page @future @creates-ride @regression >> Verify that no critical JavaScript errors occur during a future booking submission
- Location: tests/on-demand/future-booking-only/future-confirmation.spec.ts:103:7

# Error details

```
TimeoutError: page.waitForURL: Timeout 60000ms exceeded.
=========================== logs ===========================
waiting for navigation until "load"
============================================================
```

# Page snapshot

```yaml
- generic [ref=e1]:
  - generic [ref=e5]:
    - generic [ref=e6]:
      - generic [ref=e7]:
        - button "back" [ref=e8] [cursor=pointer]:
          - img "back" [ref=e9]
        - heading "Confirm Location & Time" [level=2] [ref=e10]
      - generic [ref=e12]:
        - generic [ref=e13]:
          - img "pickup" [ref=e20]
          - generic [ref=e21]:
            - textbox "Pick-up from?" [ref=e22]: Automated OD Future Booking
            - img "clear" [ref=e24] [cursor=pointer]
        - generic [ref=e25]:
          - img "dropoff" [ref=e27]
          - generic [ref=e28]:
            - textbox "Where to?" [ref=e29]: Terminal 5E
            - img "clear" [ref=e31] [cursor=pointer]
    - generic [ref=e33]:
      - generic [ref=e34]:
        - generic [ref=e35]:
          - generic:
            - region "Map" [ref=e36]
            - generic "Map marker" [ref=e37]:
              - img "Pick-up" [ref=e39]
            - generic "Map marker" [ref=e40]:
              - generic [ref=e41]: Pick-up
            - generic "Map marker" [ref=e42]:
              - img "Drop-off" [ref=e44]
            - generic "Map marker" [ref=e45]:
              - generic [ref=e46]: Drop-off
          - group [ref=e47]:
            - generic [ref=e48]:
              - link "© MapTiler" [ref=e49] [cursor=pointer]:
                - /url: https://www.maptiler.com/copyright/
              - link "© OpenStreetMap contributors" [ref=e50] [cursor=pointer]:
                - /url: https://www.openstreetmap.org/copyright
        - generic [ref=e52]:
          - button [ref=e53] [cursor=pointer]:
            - img [ref=e54]
          - button [ref=e57] [cursor=pointer]:
            - img [ref=e58]
      - generic [ref=e60]:
        - generic [ref=e62]:
          - generic [ref=e63]:
            - button "back" [ref=e64] [cursor=pointer]:
              - img "back" [ref=e65]
            - generic [ref=e66]: Enter Ride Details
          - generic [ref=e67]:
            - generic [ref=e68]: Invalid time slot
            - generic [ref=e69]:
              - heading "Rider Details" [level=4] [ref=e70]
              - textbox "Name *" [ref=e73]: PW_Rider_d7t8
              - generic [ref=e75]:
                - generic [ref=e77] [cursor=pointer]: "+91"
                - textbox "Phone number *" [ref=e79]: "8676913831"
            - generic [ref=e80]:
              - heading "Other Details" [level=4] [ref=e81]
              - generic [ref=e83]:
                - img "Assistance" [ref=e84]
                - text: Special Assitance
              - textbox "Note to Driver" [ref=e89]
              - textbox "Flight Number" [ref=e92]
              - textbox "Room Number *" [ref=e95]: R166
          - generic [ref=e96]:
            - button "Request Ride" [active] [ref=e97] [cursor=pointer]
            - paragraph [ref=e98]:
              - text: You agree to our
              - link "Terms of Services" [ref=e99] [cursor=pointer]:
                - /url: https://php-staging.trackmyshuttle.com/tos
              - text: and
              - link "Privacy policy" [ref=e100] [cursor=pointer]:
                - /url: https://php-staging.trackmyshuttle.com/privacy
        - generic [ref=e101]:
          - generic [ref=e103]:
            - generic [ref=e107]:
              - textbox "Pick-up Date" [ref=e108]: Today
              - img [ref=e110]
              - group
            - generic [ref=e113]:
              - textbox "Pick-up Time" [ref=e115]
              - img "clock" [ref=e117]
          - generic [ref=e121]:
            - generic [ref=e122]:
              - text: No. of Riders
              - subscript [ref=e123]: "*"
            - generic [ref=e124]:
              - combobox "No. of Riders * 7 Riders" [ref=e125] [cursor=pointer]: 7 Riders
              - textbox: "7"
              - img
              - group:
                - generic: No. of Riders *
          - button "Next" [ref=e130] [cursor=pointer]
  - alert [ref=e132]
```

# Test source

```ts
  60  |   }
  61  | 
  62  |   async waitForFormVisible() {
  63  |     await expect(this.formTitle).toBeVisible({ timeout: RIDER_TIMEOUTS.FORM_LOAD });
  64  |   }
  65  | 
  66  |   async fillName(name: string) {
  67  |     await this.nameInput.click();
  68  |     await this.nameInput.fill(name);
  69  |   }
  70  | 
  71  |   async fillPhone(phone: string) {
  72  |     await this.phoneInput.click();
  73  |     await this.phoneInput.fill(phone);
  74  |   }
  75  | 
  76  |   async selectCountryCode(countryName: string) {
  77  |     await this.countryCodeDisplay.click();
  78  |     await expect(this.countryCodeDropdown).toBeVisible();
  79  |     await this.countrySearchInput.fill(countryName);
  80  |     await this.page.getByText(countryName, { exact: true }).first().click();
  81  |   }
  82  | 
  83  |   /** Select riders from MUI dropdown (skips if not visible — already on location page) */
  84  |   async selectRiders(count: number) {
  85  |     const isVisible = await this.ridersDropdown.isVisible({ timeout: 2_000 }).catch(() => false);
  86  |     if (!isVisible) return;
  87  |     await this.ridersDropdown.scrollIntoViewIfNeeded();
  88  |     await this.ridersDropdown.evaluate((el) => el.dispatchEvent(new MouseEvent('mousedown', { bubbles: true })));
  89  |     const option = this.page.getByRole('option', { name: new RegExp(`^${count} Rider`) }).first();
  90  |     await option.waitFor({ timeout: RIDER_TIMEOUTS.STOP_LIST });
  91  |     await option.click();
  92  |   }
  93  | 
  94  |   async toggleSpecialAssistance() {
  95  |     await this.page.locator('label[for="checkBoxOne"]').click();
  96  |   }
  97  | 
  98  |   async fillNotes(text: string) {
  99  |     await this.notesTextarea.scrollIntoViewIfNeeded();
  100 |     await this.notesTextarea.fill(text);
  101 |   }
  102 | 
  103 |   /** Fill the ASAP "Flight Number" field (maxlength 10). */
  104 |   async fillFlight(value: string) {
  105 |     await this.flightInput.scrollIntoViewIfNeeded();
  106 |     await this.flightInput.fill(value);
  107 |   }
  108 | 
  109 |   /** Fill the ASAP "Room Number" field (maxlength 10). */
  110 |   async fillRoom(value: string) {
  111 |     await this.roomInput.scrollIntoViewIfNeeded();
  112 |     await this.roomInput.fill(value);
  113 |   }
  114 | 
  115 |   /**
  116 |    * Fill required fields using org config from environment.
  117 |    * Uses the phone number and country code from rider-config.ts.
  118 |    */
  119 |   async fillRequiredFields(overrides: Partial<{ name: string; phone: string; countryCode: string; riders: number }> = {}) {
  120 |     const config = getRiderConfig();
  121 |     const org = Object.values(config.orgs).find(o => o.enabled) as OrgModeConfig;
  122 |     const ts = Date.now().toString(36).slice(-4);
  123 |     const name = overrides.name ? `PW_${overrides.name}_${ts}` : `PW_Rider_${ts}`;
  124 |     const phone = overrides.phone || org.phone.number;
  125 |     const countryCode = overrides.countryCode || org.phone.countryCode;
  126 |     // Random rider count between 1-3 if not specified
  127 |     const riders = overrides.riders ?? Math.floor(Math.random() * 3) + 1;
  128 | 
  129 |     await this.selectCountryCode(countryCode);
  130 |     await this.fillName(name);
  131 |     await this.fillPhone(phone);
  132 |     await this.selectRiders(riders);
  133 | 
  134 |     // Return the values actually entered so callers can assert them downstream
  135 |     // (e.g. the rider-details card on the tracking screen).
  136 |     return { name, phone, countryCode, riders };
  137 |   }
  138 | 
  139 |   async submitForm() {
  140 |     await this.requestRideButton.click();
  141 |   }
  142 | 
  143 |   /**
  144 |    * Submit the ride form and wait for the tracking page (/j/{code}/s).
  145 |    *
  146 |    * Hardens the silent-hang failure mode: instead of clicking once and blindly
  147 |    * waiting up to RIDE_SUBMIT for a navigation that may never come (dropped click
  148 |    * during a re-render, or a rate-limited/failed POST), we:
  149 |    *   1. assert the button is enabled + stable before the single click (no double-submit),
  150 |    *   2. race the navigation against a visible error toast so a failed submit
  151 |    *      surfaces a clear error fast instead of timing out blindly.
  152 |    */
  153 |   async submitAndAwaitTracking(timeout: number = RIDER_TIMEOUTS.RIDE_SUBMIT) {
  154 |     await expect(this.requestRideButton).toBeEnabled({ timeout: RIDER_TIMEOUTS.FORM_LOAD });
  155 |     await this.requestRideButton.scrollIntoViewIfNeeded();
  156 |     // Single click only — never re-click once a POST may be in flight.
  157 |     await this.requestRideButton.click();
  158 | 
  159 |     const navigated = this.page
> 160 |       .waitForURL(/\/j\/.*\/s/, { timeout })
      |        ^ TimeoutError: page.waitForURL: Timeout 60000ms exceeded.
  161 |       .then(() => 'navigated' as const);
  162 |     const errorToast = this.page.locator('.Toastify__toast--error').first();
  163 |     const errored = errorToast
  164 |       .waitFor({ state: 'visible', timeout })
  165 |       .then(() => 'error' as const)
  166 |       .catch(() => 'no-error' as const);
  167 | 
  168 |     const outcome = await Promise.race([navigated, errored]);
  169 |     if (outcome === 'error') {
  170 |       const msg = (await errorToast.textContent().catch(() => ''))?.trim();
  171 |       throw new Error(`Ride submission failed — error toast shown: "${msg}"`);
  172 |     }
  173 |     // Ensure the navigation actually completed (outcome may be 'navigated' already).
  174 |     await navigated;
  175 |   }
  176 | 
  177 |   async clickBack() {
  178 |     // The location-page back button owns the "Go Back?" confirmation, but under
  179 |     // load the form overlay can intercept pointer events at its coordinates
  180 |     // (the element is visible/stable, yet the hit-test — and a coordinate-based
  181 |     // force click — lands on the overlay instead). Try a real click first; if it
  182 |     // times out, dispatch the click directly on the DOM node so React's onClick
  183 |     // still fires regardless of what visually covers the button.
  184 |     await this.backButton.scrollIntoViewIfNeeded().catch(() => {});
  185 |     try {
  186 |       await this.backButton.click({ timeout: RIDER_TIMEOUTS.MUI_DROPDOWN * 8 });
  187 |     } catch {
  188 |       await this.backButton.evaluate((el) => (el as HTMLElement).click());
  189 |     }
  190 |   }
  191 | 
  192 |   /** Check if name input has validation error (borderError class) */
  193 |   async hasNameError(): Promise<boolean> {
  194 |     const classes = await this.nameInput.getAttribute('class') ?? '';
  195 |     return classes.includes('borderError');
  196 |   }
  197 | 
  198 |   /** Check if phone input has validation error */
  199 |   async hasPhoneError(): Promise<boolean> {
  200 |     const classes = await this.page.locator('input[placeholder="Phone number *"]').getAttribute('class') ?? '';
  201 |     return classes.includes('borderError');
  202 |   }
  203 | 
  204 |   /** Check if riders dropdown has validation error */
  205 |   async hasRidersError(): Promise<boolean> {
  206 |     const riderDrop = this.page.locator('.rider-drop');
  207 |     if (!(await riderDrop.isVisible({ timeout: 1_000 }).catch(() => false))) return false;
  208 |     const classes = await riderDrop.getAttribute('class') ?? '';
  209 |     return classes.includes('borderError');
  210 |   }
  211 | 
  212 |   /** Verify ASAP form state — correct fields visible/hidden */
  213 |   async verifyAsapFormState() {
  214 |     await expect(this.nameInput).toBeVisible();
  215 |     await expect(this.phoneInput).toBeVisible();
  216 |     await expect(this.specialAssistanceCheckbox).toBeVisible();
  217 |     await expect(this.notesTextarea).toBeVisible();
  218 |     // Flight & Room are org-configurable "Other Details" fields: present on some
  219 |     // orgs (e.g. preprod/prod ODASAP) and absent on others (staging ODASAP), so
  220 |     // assert them only when the org actually renders them.
  221 |     if (await this.flightInput.count() > 0) {
  222 |       await expect(this.flightInput).toBeVisible();
  223 |       await expect(this.roomInput).toBeVisible();
  224 |     }
  225 |     // Rider-type radios remain hidden in ASAP mode.
  226 |     await expect(this.riderTypeSection).not.toBeVisible();
  227 |   }
  228 | 
  229 |   /** Get rider option count from MUI dropdown */
  230 |   async getRiderOptionCount(): Promise<number> {
  231 |     await this.ridersDropdown.scrollIntoViewIfNeeded();
  232 |     await this.ridersDropdown.evaluate((el) => el.dispatchEvent(new MouseEvent('mousedown', { bubbles: true })));
  233 |     await this.page.getByRole('option').first().waitFor({ timeout: RIDER_TIMEOUTS.STOP_LIST });
  234 |     const count = await this.page.getByRole('option').count();
  235 |     await this.page.keyboard.press('Escape');
  236 |     return count;
  237 |   }
  238 | }
  239 | 
```